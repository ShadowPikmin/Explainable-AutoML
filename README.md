# Explainable-AutoML
Seminar Research Proejct attempting to create a model that explain why certain algorithms are choose by the AutoML 

Installation: 
python3.11 -m tpot_env_311 .venv
source tpot_env_311/bin/activate

pip install --upgrade pip
pip install setuptools==68.2.2
pip install wheel

pip install numpy pandas scikit-learn
pip install openml pymfe
pip install tpot
pip install shap
pip3 install torch torchvision torchaudio
pip install bayesian-optimization==1.3.0